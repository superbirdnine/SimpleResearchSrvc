# Draft implementation plan

1. Extract only portable routing and portal behavior from the reference system.
2. Define one canonical workflow and thin Codex/Claude adapters.
3. Add a safe intake CLI, templates, synthetic notes, and taxonomy checks.
4. Implement a configurable read-only Next.js portal.
5. Cover routing, traversal protection, search indexing, stable URLs, Markdown, local assets, and external-link behavior.
6. Validate with lint, tests, build, a running portal smoke check, and a fresh-location install.
