# Status

Phase: implemented and validated

The standalone starter, canonical workflow, example knowledge roots, Fieldnotes portal, and automated acceptance coverage are present. See `docs/clean-install.md` for the release verification procedure.

Known scope boundary: source recovery is performed by the active agent/browser tool; the bundled CLI validates and persists already-recovered evidence but does not automate arbitrary browser sessions.
